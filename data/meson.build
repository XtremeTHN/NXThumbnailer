mime_types_out = get_option('prefix') / 'share' / 'mime' / 'packages/'
thumnailer_out = get_option('prefix') / 'share' /'thumbnailers'

install_data(
    'nxthumbnail.xml',
    install_dir: mime_types_out,
)
install_data(
    'nxthumbnail.thumbnailer',
    install_dir: thumnailer_out
)