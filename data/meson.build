mime_types_out = get_option('prefix') / 'mime' / 'packages/'
thumnailer_out = get_option('prefix') / 'thumbnailers'

install_data(
    'nxroms.xml',
    install_dir: mime_types_out,
)
install_data(
    'nxroms.thumbnailer',
    install_dir: thumnailer_out
)